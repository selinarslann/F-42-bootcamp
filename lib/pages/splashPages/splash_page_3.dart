import 'package:flutter/material.dart';

class SplashPageTree extends StatelessWidget {
  const SplashPageTree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Splash Page Tree'),
      ),
    );
  }
}
